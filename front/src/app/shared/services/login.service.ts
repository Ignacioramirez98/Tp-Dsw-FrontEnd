// login.service.ts (Angular)
/*import { Injectable } from '@angular/core';
import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';

@Injectable({
  providedIn: 'root',
})
export class LoginService {
  private apiUrl = 'http://localhost:3000/api/login'; // Asegúrate de que la URL sea correcta

  constructor(private http: HttpClient) {}

  login(usuario: string, password: string, userType: string): Observable<any> {
    return this.http.post<any>(this.apiUrl, { usuario, password, userType });
  }
}
*/